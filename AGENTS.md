# Agent 规则

## 文件修改限制

- **可以直接修改**项目中的源代码文件（如 `.py`、`.js`、`.vue`、`.ts` 等），使用 `WriteFile` 或 `StrReplaceFile` 工具直接编辑
- 每次修改后，必须在 `/Users/ty/Downloads/satellite 2/changes.md` 中追加一条**简单的修改总结**（1-3句话即可），包含：修改了哪个文件、改了什么、为什么改
- **禁止覆盖或删除 `changes.md` 的已有内容**：所有新总结通过 `append` 模式追加到文件末尾
- 如果用户明确要求"只写入changes.md不修改源文件"，则恢复为原模式（代码块形式写入changes.md）

## 代码与修改规范

- **最小修改原则**：只改动与任务直接相关的代码，不重构无关逻辑，不引入未请求的优化
- **先读后改**：修改前先通过 `ReadFile` 或 `Grep` 读取并理解相关文件内容，避免盲目猜测
- **保持一致性**：新增代码的风格（缩进、命名、引号、分号等）必须与周围现有代码严格一致
- **禁止硬编码敏感信息**：密码、密钥、Token 等必须提醒用户自行配置，不得写入代码
- **每次改动必须附带测试用例**：任何功能新增、Bug 修复或逻辑变更，都必须在 `changes.md` 中提供对应的测试代码或测试步骤；测试用例需覆盖正常路径和至少一个异常/边界场景
- **修改总结格式示例**：`【修改】backend/app.py：新增 /register/ 路由，修复前端注册报 404 的问题。同步更新 UserModel.py 添加 user_type 字段以匹配数据库表结构。`

## 安全与边界

- **禁止**执行任何可能破坏数据的命令（如 `rm -rf`、`drop table`、无过滤的 `truncate` 等）
- **禁止**修改工作目录之外的任何文件（系统文件、用户配置文件等）
- 涉及删除文件或覆盖数据的操作，必须先向用户确认

## 输出与沟通

- 修改前简要说明思路，修改后说明改动要点和影响范围
- 如果任务复杂或涉及多文件改动，使用 `SetTodoList` 跟踪进度
- 发现需求不明确时，先询问用户确认，不要假设猜测
- 每次写入 `changes.md` 时，使用清晰的 Markdown 格式，包含：文件路径、改动说明、完整代码块
- **禁止覆盖或删除 `changes.md` 的已有内容**：所有新内容必须通过 `append` 模式追加到文件末尾，或在文件末尾添加新的章节分段，确保历史记录完整保留
