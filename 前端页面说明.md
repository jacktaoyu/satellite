# 智能星簇协同运行验证系统 —— 前端页面内容与功能说明

> 用途：本文档完整描述现有系统的所有前端页面（结构、数据、图表、交互、接口），可直接提供给 AI 作为重新设计/生成新页面的需求输入。
> 技术栈现状：Vue 3 + Element Plus + ECharts + Cesium，后端 Flask（http://127.0.0.1:5001），鉴权方式为请求头 `Ac-Token`。
> 系统主题：卫星星簇（卫星集群）在轨运行的三维可视化监控、任务规划与调度、效能评估验证平台。

---

## 一、系统整体结构

### 站点地图（路由）

| 路径 | 页面 | 说明 | 权限 |
|---|---|---|---|
| `/portal` | 首页门户 | 对外落地页/系统入口 | 公开 |
| `/login` | 登录页 | 独立登录页 | 公开 |
| `/register` | 注册页 | 用户注册 | 公开 |
| `/satellite/satellite_network` | 卫星网络态势大屏 | 核心 3D 监控页 | 管理员 |
| `/satellite/system_settings` | 系统设置 | 参数/权重/TLE/卫星参数配置 | 管理员 |
| `/satellite/network_parameters` | 按载荷批量设置 | 隐藏页，仅从系统设置跳转 | 管理员 |
| `/satellite/weixing` | 卫星管理 | 卫星列表 CRUD | 管理员 |
| `/satellite/Weixing/info/:name` | 卫星详情 | 单星全部参数与实时状态 | 管理员 |
| `/satellite/renwu/shuxing` | 任务属性（任务管理） | 任务全生命周期管理 | 管理员 |
| `/satellite/renwu/shezhi` | 任务设置 | 批量导入任务/快速生成用例 | 管理员 |
| `/satellite/xingcu` | 星簇管理 | 星簇 CRUD/重规划/批量导入 | 管理员 |
| `/satellite/ground_station` | 地面站 | 地面站列表与链路状态 | 管理员 |
| `/satellite/yongli` | 示范用例 | 一键执行典型场景 | 管理员 |
| `/satellite/xingneng` | 性能分析 | 规划算法评估与对比 | 管理员 |

### 主框架（登录后的后台布局）

- 左侧深色侧边栏（宽 200px，#011528），logo 文字"智能星簇协同运行验证系统"
- 菜单项：卫星网络 / 系统设置 / 卫星管理 / 任务管理（任务属性、任务设置）/ 星簇管理 / 地面站 / 示范用例 / 性能分析
- 顶栏：折叠按钮、头像+昵称、下拉菜单（个人中心[disabled]、个人设置[修改密码弹窗]、退出登录）
- 修改密码弹窗：账号/新密码/确认密码 → POST `/updatePassword`
- 主区域：卫星网络页全屏无 padding，其余页面 padding 20px，底色 #0b1530

### 当前视觉风格（不统一，重设计时建议统一）

- 深色科技风（青色发光 #00dcff、深蓝底 #011528/#0b1530）：首页门户、登录、卫星网络大屏、地面站、按载荷批量设置
- Element Plus 默认浅色后台风：卫星管理、卫星详情、任务属性、任务设置、星簇管理、示范用例、系统设置
- 性能分析：浅色卡片 + 深色 HUD 图表（混搭）

---

## 二、各页面详细内容

### 1. 首页门户 `/portal`

**用途**：对外门户落地页，系统入口。

**布局**：全屏单页
- 顶部导航：logo + 标题"智能星簇协同运行验证系统" + 右侧"进入系统"按钮
- Hero 区：左侧文字（徽章动画、大标题"智能星簇/协同运行验证系统"、描述、"进入系统"按钮、数据看板），右侧 3D 地球
- 页脚：© 2026 智能星簇协同运行验证系统 · Satellite Cluster Collaborative Platform

**数据看板**（数字滚动动画）：在轨卫星(颗)、今日任务(次)、待执行任务(次)、在线客户端(个) —— GET `/statistics`

**可视化**：Cesium 3D 地球背景（高德瓦片、禁用交互、昼夜光照+bloom、视角在亚洲昼半球±20°摆动）、5 颗装饰绕轨发光卫星+轨道环、3 个装饰地面站（北京/喀什/三亚）+呼吸光点+星地链路光束、Canvas 星空流星粒子层、扫描线、HUD 四角框、遥测读数装饰、鼠标视差

**交互**："进入系统"→ 相机推进后弹出登录弹层（内嵌登录卡片），Esc/遮罩/✕ 关闭并飞回原位

### 2. 登录页 `/login`

全屏 Cesium 地球背景（缓慢自转、不可交互）；左侧标语区（徽章"SATELLITE CLUSTER COLLABORATIVE PLATFORM"、标题、副标"三维组网可视化 · 任务协同规划 · 运行效能评估"，窄屏隐藏）；右侧登录卡片；左上角"← 返回首页"

**登录卡片**（首页弹层复用同一组件）：
- 表单：账号、密码（回车提交）、用户类型下拉（管理员/用户）
- 按钮"登 录"、链接"还没有账号？立即注册"
- POST `/login/`（username/password/value），成功后 7 个字段写 localStorage，跳转 `/satellite/satellite_network`

### 3. 注册页 `/register`

背景图 + 居中白色半透明卡片；表单：用户名（3-20字符）、密码（≥6位）、确认密码（一致性校验）；按钮"注册"、"返回登录"；固定注册为普通用户 → POST `/register/`，成功跳登录页

### 4. 卫星网络态势大屏 `/satellite/satellite_network`（核心页面）

**用途**：Cesium 3D 全球态势监控，全屏。

**布局**：
- 中央：Cesium 地球（高德瓦片、仅保留时间轴控件、中央雷达环+十字线装饰）
- 顶部标题栏：左侧 4 指标（在线卫星数、正在执行任务数、任务满足率%、规划耗时s）；居中标题"智能星簇协同运行验证系统 - 卫星网络态势监控"；右侧"仿真时间 HH:MM:SS"
- 左侧面板（230px）：
  - 实时卫星列表（N）：勾选框（控制3D显隐）、卫星名、载荷类型、电量(Wh)，点击 flyTo 聚焦
  - 任务执行进度（N）：任务名+进度条（紧急任务黄色）
  - 载荷类型分布：ECharts 环形饼图（光学青/红外黄/SAR绿）
  - 常用功能 4 开关：全部轨迹、全部视锥体、实时事件栏、通信链路
- 右侧面板（230px）：
  - 卫星详情：纬度°、经度°、高度km、载荷类型、分辨率m、电量Wh、存储GB、周期min、时间、俯仰角°、侧摆角°、相连地面站、相连高轨卫星；开关"是否显示路径/视锥体"、按钮"隐藏"
  - 任务状态统计：ECharts 柱状图（等待规划/正在执行/已完成）
  - 任务满足率趋势：ECharts 平滑面积折线（最近10次规划评估）
- 底部：实时事件滚动栏（最多30条：新任务/任务状态变更/低电量告警[<20Wh]/规划完成；警告黄色、告警红色）

**3D 场景**：CZML 卫星实体（青色发光轨迹+拖尾+50°视锥体）、5 个地面站（重庆/雄安/喀什/文昌/佳木斯，300km半径半球+旋转墙）、站间连线（雄安枢纽）、3 颗高轨中继卫星（东经120°/西经120°/0°，35786km）、星间链路（卫星→GEO，橙色虚线，拉远才显示）、星地数传链路（亮青实线）

**接口**：GET `/getCurrentTime`、POST `/satellites/getAllSatellites`、POST `/satellites/getAllSatelliteInfo`、GET `/tasks/getNewTasksByCondition`、GET `/getPlanningEvaluation`、GET `/getCzml`

**轮询**：每 5 秒整体刷新

### 5. 卫星管理 `/satellite/weixing`

**布局**：搜索工具栏 → 批量操作栏（选中时出现）→ 表格 → 分页

- 搜索区：卫星名称输入、载荷类型下拉（SAR/光学/红外）、状态下拉（可用/不可用）、"搜索""重置"；右侧"导出全部""刷新"
- 批量操作："已选择 N 颗卫星"、"批量启用/批量禁用/批量导出"、"清空选择"
- 表格列：多选、ID、卫星名称、轨道、载荷(Tag着色)、电池Wh(可排序)、存储GB(可排序)、分辨率m、操作（详情/编辑/导出）
- 编辑弹窗"编辑卫星参数"：基础参数（存储容量GB、电池容量Wh、下行速率GB/s）、功率参数（空闲/太阳能/机动/成像功率W）、载荷参数（角度转动速度°/s、稳定时间s、最大侧摆角度°、最大俯仰角度°、云层厚度阈值m）

**接口**：POST `/satellites/getAllSatellites`、GET `/satellites/getSatelliteById/{id}`、POST `/satellites/setSatelliteProperty/{id}`、POST `/satellites/setSatelliteAvailable/{id}`、POST `/satellites/setSatelliteUnavailable/{id}`、GET `/satellites/exportSatelliteInfo/{id}`（xlsx）、GET `/satellites/exportAllSatelliteInfo`

### 6. 卫星详情 `/satellite/Weixing/info/:name`

**布局**：头部卡（图标+名称+载荷/状态/轨道标签 + 返回/编辑/导出按钮）→ 两列（基础信息、实时状态）→ 两列（资源信息、功率参数）→ 载荷参数网格

- 基础信息：卫星ID、名称、轨道、载荷类型、分辨率m、幅宽km、状态开关（直接调启停接口）
- 实时状态：卫星位置、速度、星下点、飞行圈数、连接的高轨卫星、执行中的任务、任务总数
- 资源信息：存储容量GB、电池容量Wh、下行速率GB/s
- 功率参数：空闲/太阳能/机动/成像功率W
- 载荷参数（8项网格）：侧摆角°、俯仰角°、载荷转动角速度°/s、稳定时间s、云层遮挡阈值m、最大侧摆角度、最大俯仰角度、角度转动速度°/s

**接口**：GET `/satellites/getSatelliteByName/{name}`、启停/保存/导出接口同上

### 7. 任务属性（任务管理）`/satellite/renwu/shuxing`

**布局**：搜索区 → 批量栏 → 4 统计卡片（总任务数/等待执行/执行中/已完成）→ Tabs（待执行任务/历史任务）→ 分页

- 搜索区：任务名称、任务类型下拉（点目标/区域目标/广域目标/移动目标/静态观测/周期观测）、载荷类型下拉、任务状态下拉（等待执行/执行中/暂停/已完成）、"搜索""重置"；右侧"新增任务""导出当前列表""刷新"
- 待执行任务表格列：多选、ID、任务名称、任务类型(Tag)、优先级(可排序)、紧急、载荷、分辨率m、分配卫星、指定星簇、合并任务ID、状态(Tag)、开始/结束时间、定时时间、区域信息；操作列（详情/启动/暂停/结束/编辑/导出/删除）
- 历史任务表格列：ID、名称、类型、优先级、执行卫星、执行结果(成功/失败)、开始/结束时间；操作（详情/导出）
- 新增/编辑弹窗：任务名称*、任务类型*、优先级(1-10)、是否紧急、载荷类型*、分辨率要求、时间范围、目标位置"[纬度, 经度]"/区域坐标点（动态增删行）、指定星簇、预约时间、周期(分钟)、云层厚度
- 详情弹窗：el-descriptions 全字段两列展示

**接口**：POST `/tasks/getNewTasks`、POST `/tasks/getOldTasks`、GET `/tasks/getNewTasksByCondition`、GET `/clusters/getAllClustersNames`、GET `/clusters/getClusterDetailsByName/{name}`、POST `/tasks/addSingleTask`、POST `/tasks/updateTask/{id}`、POST `/tasks/startTask/{id}`、POST `/tasks/pauseTask/{id}`、POST `/tasks/manualEndTask/{id}`、DELETE `/tasks/deleteTask/{id}`、GET `/tasks/exportTask/{id}`、GET `/tasks/exportAllNewTasks` 等

**轮询**：列表每 5 秒刷新（弹窗打开时暂停）

### 8. 任务设置 `/satellite/renwu/shezhi`

左右两列卡片：
- 左"批量导入任务"：拖拽上传区（.xlsx/.xls/.csv/.json）、"确认上传/清空"、导入说明（字段：taskName、taskType、priority、isEmergency、sensorType、resolution、targetLocation、clusterName可选）
- 右"快速生成用例"：3 个条目——点目标案例/区域目标案例/海洋搜救案例，各有"生成"按钮
- 生成结果卡片：用例类型、任务ID、任务数量、生成时间、任务状态、载荷类型、任务类型、执行卫星、目标位置

**接口**：POST `/tasks/addTasks`、GET `/tasks/pointTargetCase`、`/tasks/areaTargetCase`、`/tasks/oceanTargetCase`

### 9. 星簇管理 `/satellite/xingcu`

- 搜索区：星簇名称 + "搜索""重置"；右侧"新增星簇""批量导入""导出全部""刷新"
- 表格列：ID、星簇名称、包含轨道、卫星数量、包含卫星(前3个Tag+"+N")、载荷及分辨率（如"SAR:0.5,1|optical:0.5"）、状态开关、操作（详情/编辑/重规划/删除）
- 新增/编辑弹窗：星簇名称*、选择轨道*(多选)、载荷配置（SAR/光学/红外 checkbox + 各自分辨率多选）
- 重规划弹窗"任务重规划"：原星簇 → 新星簇（只显示轨道/载荷适配的可用星簇）、"确认迁移"
- Excel 批量导入弹窗：拖拽上传 .xlsx（提示"覆盖现有星簇"）

**接口**：POST `/clusters/getClustersByPage`、GET `/clusters/getOrbits`、POST `/clusters/getInfoByOrbits`、POST `/clusters/addCluster`、POST `/clusters/updateCluster/{id}`、DELETE `/clusters/deleteClusterById/{id}`、POST `/clusters/setAvailableCluster/{id}`、POST `/clusters/replan`、GET `/clusters/exportAllClusters`、POST `/clusters/submitClusterFile`

### 10. 地面站 `/satellite/ground_station`

深色 HUD 风格。顶部标题卡（"地面站管理 / GROUND STATION MANAGEMENT" + 指标：地面站总数、当前链路数 + "刷新"按钮）→ 表格

- 表格列：expand展开行（连接卫星Tags）、站名、经度°、纬度°、链路状态（"链路已建立"/"空闲待命"Tag）、当前连接卫星数、连接卫星列表

**接口**：GET `/satellites/groundStationInfo`（name、location=[纬度,经度]、connecting_satellite[]）

### 11. 示范用例 `/satellite/yongli`

**布局**：头部卡（"示范用例 执行典型任务场景，验证系统能力" + "刷新状态"）→ 三张用例卡片 → 执行结果卡 → 执行历史

- 用例卡片：
  - 点目标案例（蓝）："生成一批点目标观测任务，测试单点观测能力"，标签：单点观测/高分辨率/快速响应
  - 区域目标案例（绿）："生成区域目标观测任务，测试大范围覆盖能力"，标签：区域覆盖/条带拼接/协同观测
  - 海洋搜救案例（橙）："生成海洋移动目标搜救任务，测试动态跟踪能力"，标签：移动目标/动态跟踪/紧急响应
  - 每卡按钮："执行用例""查看详情"
- 用例详情弹窗：用例说明、案例参数（优先级/是否紧急/载荷类型/分辨率/所属星簇/云层厚度/时间范围/坐标点Tags）、最近执行结果、**案例区域示意**（Cesium 2D 地图：预置点+边界多边形+目标轨迹）、任务特点、执行流程步骤条（生成任务→任务规划→卫星执行→结果反馈）
- 执行历史：el-timeline（用例名、成功/失败、生成N个任务、时间戳）、"清空历史"

**接口**：GET `/tasks/pointTargetCase|areaTargetCase|oceanTargetCase`、GET `/tasks/presetCaseInfo/{type}`、GET `/tasks/caseResult/{type}`、POST/GET/DELETE `/tasks/caseHistory`

### 12. 性能分析 `/satellite/xingneng`

两个 Tab：

**Tab1 算法性能分析**：卡头显示"当前系统模式：xxx"（自动/任务完成度最高/资源利用率最大/成像质量最高）+"当前系统时间"，星簇下拉；4 宫格 ECharts 平滑折线（X轴为时间）：任务数量、电量消耗量(Wh)、固存使用量(GB)、固存资源利用率(%)

**Tab2 系统性能分析**：
- 导出按钮："导出贪心算法数据""导出蚁群算法数据""导出遗传算法数据""导出当前方案数据"（txt）
- 4 统计卡：任务完成率%、资源利用率%（三算法均值）、平均规划耗时s、规划次数
- "多算法性能对比"4 宫格折线：任务完成率%、电量消耗量Wh、固存使用量GB、规划耗时s；4 系列：蚁群/遗传/贪心/当前方案
- 雷达图"算法评估指标"（完成率/资源利用率/成像质量/响应速度，可切换贪心/蚁群/遗传）+ 柱状图"任务执行统计"
- "详细评估数据"表格：算法、完成率%、资源利用率(进度条)、成像质量%、电量消耗、存储消耗、规划耗时、评估时间、详情

**接口**：GET `/getPlanningEvaluation`、GET `/getModel`、GET `/getCurrentTime`、GET `/exportScheduleStatus`、GET `/clusters/getAllClustersNames`、GET `/getClusterData/{clusterName}`、POST `/exportSchedule/{greedy|ant|genetic|schedule}`

**轮询**：仿真时间每 5 秒刷新

### 13. 系统设置 `/satellite/system_settings`

浅色背景，两个分区：

**分区 01 系统参数设置**：
- 左卡"时间和参数设置"：系统仿真时间（起止时间范围）、算法权重配置（任务完成率/负载均衡/优先级权重，0-10步0.1）；"保存并应用""重置"
- 右上卡"系统模式"：手动/自动模式 switch、状态条（TLE 已上传/未上传、卫星参数 已上传/未上传、系统参数 已提交/待提交）、手动模式下"调度方案"单选（任务完成度最高/资源利用率最大/成像质量最高）
- 右下卡"TLE 轨道数据"（必需）：上传 .txt/.tle 文件、"选择文件""导出轨道数据"

**分区 02 卫星参数配置**：
- "批量导入卫星参数"卡（必需）：拖拽上传 .json/.txt/.xls/.xlsx/.csv ≤10MB、"确认上传/清空"
- "单个卫星参数设置"卡：卫星下拉（名称(轨道-载荷)），选中展开表单——基础参数（存储容量GB、电池容量Wh、下行速率GB/s）、功率参数（空闲/太阳能/机动/成像功率W）、载荷参数（载荷类型[可见光/红外/SAR]、分辨率m、幅宽km、角速度°/s、稳定时间s、最大侧摆角°、最大俯仰角°、云层厚度阈值m）；"保存设置""添加到列表""重置"
- "数据列表（N条记录）"卡：17 列批量参数表格、"清空""提交全部参数"

**接口**：GET `/isSubmitTle|isSubmitSat|isSubmitSys`、POST `/simulateParameters`、POST `/initTLE`、POST `/initFiles`、GET `/exportTleFile`、POST `/satellites/getAllSatellites`、GET `/satellites/getSatelliteById/{id}`、POST `/satellites/setSatelliteProperty/{id}`、POST `/networkParametersList`

### 14. 按载荷批量设置 `/satellite/network_parameters`（隐藏页）

深蓝渐变独立页。三个传感器类型卡片切换（光学📸/SAR📡/红外🌡️）→ 参数表单（三列网格，每种载荷一套共14项）：最大存储MB、最大电池Wh、分辨率m、俯仰角°、侧摇角°、稳定时间s、角速度°/s、条带宽度km、阈值、下行速率Mbps、阳光功率W、机动功率W、成像功率W、日影功率W；底部"重置""提交配置"

**接口**：POST `/networkParameters`（body 为 optical/SAR/infrared 三个参数对象）

---

## 三、全局公共能力

- **鉴权**：登录后所有请求带 `Ac-Token` 头；401 统一清登录态跳登录页
- **角色**：仅区分管理员/普通用户，普通用户登录后无可用菜单
- **数据导出**：卫星/任务/星簇/算法方案均支持导出 xlsx/txt
- **仿真概念**：系统按"仿真时间"推进（可调倍速），卫星轨迹由 TLE+SGP4 实时计算，任务经规划算法（贪心/蚁群/遗传）分配后由卫星客户端执行并回传结果

## 四、重设计建议方向（供参考）

1. 统一视觉风格（当前深色科技风与浅色后台风混搭）
2. 统一字段单位（GB vs MB、GB/s vs Mbps 在不同页面不一致）
3. 管理类表格页（卫星/任务/星簇）结构高度相似，可统一为同一套列表页范式
4. 卫星网络大屏是核心亮点页，可重点强化
