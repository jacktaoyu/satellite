# 卫星星簇协同运行验证系统 — 功能验证报告（技术说明书 3.3 章逐项实测）

- 验证时间：2026-08-30 12:40 ~ 13:15（UTC+8）
- 验证方式：只验证、不改源代码。接口用 curl 实测，数据库用 `mysql -u root -proot satellite` 交叉验证，UI 用 Playwright 巡检脚本。
- 环境：后端 Flask/waitress @ 127.0.0.1:5001，前端 Vite @ 127.0.0.1:5173，MySQL satellite 库。
- 重要前置发现：后端卫星网络线程会阻塞等待 NODES=2 个客户端连接 9999 端口后才推进仿真（`is_trace=True`、SGP4 实时位置计算、任务规划）。本次验证启动了 `backend/run_mock_clients.py` 拉起 2 个模拟客户端，验证后已停止。
- 测试数据：全部使用 TEST_ 前缀或当场可识别的临时数据，测试后均已删除/清理（详见各项）。

---

## A. 其他功能（说明书 3.3.5）

### 1. 登录 —— ✅ 通过
- `POST /login/` admin/123456 → 200，`meta.status=200`，返回 token/user_id/isAdmin。
- 错误密码 → **401** `{"message":"用户名或密码错误"}`。
- 无 token 访问受保护接口 → 401 `{"meta":{"status":401,"message":"未登录或登录已过期"}}`（全局鉴权生效）。

### 2. 修改密码 —— ✅ 通过
- `POST /register/` 注册测试用户 TEST_pwd → 200（user_id=9）。
- `POST /updatePassword`（body: username/password/confirmPassword，携带本人 token）→ `{"message":"修改成功"}`。
- 新密码 654321 登录成功；旧密码 123456 登录返回 401。
- 已改回原密码，测试用户 TEST_pwd 已在验证结束后从 t_user 删除（`select id,name from t_user` 确认无残留）。

### 3. 模式切换 —— ✅ 通过
- `POST /changeModel` 依次切换 0/1/2/3（自动/任务完成度最高/资源利用率最大/成像质量最高），`GET /getModel` 分别读回 `{"mode":0/1/2/3}`，全部一致。
- 非法值 5 → 400 `{"error":"非法的算法模式，应为0-3的整数"}`。
- 已恢复为模式 0。

---

## B. 初始化（说明书 3.3.3 + 3.7.6）

### 4. GET /isSubmitTle —— ✅ 通过
- 返回 `{"is_submit_tle":true}`。

### 5. GET /exportTleFile —— ✅ 通过
- 200，Content-Type: text/plain，30320 字节，内容含标准 TLE 行（`Sat_10_0` / `1 10000 ...` / `2 10000 053.0660 ...`）。

### 6. POST /simulateParameters —— ✅ 通过（附观察）
- body `{"date1":"2026-08-30 09:00:00","date2":"2026-08-31 09:00:00","completed_gravity":0.5,"balance_gravity":0.3,"priority_gravity":0.2}` → 200 `simulate parameters received successfully`。
- 异常路径：date1 传 `"bad"` → 400 `{"error":"date1/date2格式错误..."}`。
- 观察：该接口更新 start_time/end_time 与三个权重，但**不会重置仿真当前时间 now_time**（`/getCurrentTime` 显示 current_time 仍从初始值 2025-06-06 00:00 起推进，start_time 已变为 2026-08-30 01:00 UTC）。属设计现状，如实记录。

### 7. POST /satellites/setSatelliteProperty/2 —— ✅ 通过
- resolution 0.75 → 0.8 → `getSatelliteById/2` 读回 0.8；再改回 0.75 → 读回 0.75。持久化生效。

---

## C. 卫星网络与卫星管理（说明书 3.3.3 + 3.7.7/3.7.8）

### 8. POST /satellites/getAllSatellites —— ✅ 通过
- 返回 **200 颗**卫星（id/name/storage/battery/resolution/orbit/loadType）。

### 9. GET /satellites/getSatelliteById/1 —— ✅ 通过
- 返回基础属性（name=Sat_10_0, orbit=第10轨道第0卫星, loadType=infrared, storage/battery/resolution…）、载荷参数（width/sideAngle/pitchAngle/angleVelocity/settlingTime/threshold）、运行状态（status=FREE, is_available, running_task, task_num）。

### 10. POST /satellites/getAllSatelliteInfo —— ✅ 通过（附前置条件）
- 200 颗卫星全部返回实时 position/speed/status，例：`Sat_10_0 position=[5898.98,-3526.52,815.21] speed=[1.75,4.31,5.99]`，且两次请求位置随时间变化 → SGP4 轨迹实时计算在运行。
- 注意：客户端连接前 position 为空串；需 mock 客户端连接后网络线程才开始推进（见报告开头"重要前置发现"）。

### 11. GET /getCzml —— ✅ 通过
- 200，约 3.8 MB，201 个 CZML packet（含 clock  interval=仿真起止时间、multiplier=60）。

### 12. 卫星信息导出 —— ✅ 通过
- `GET /satellites/exportSatelliteInfo/1` → 200，spreadsheetml.sheet，5394 字节，`file` 识别为 Microsoft Excel 2007+。
- `GET /satellites/exportAllSatelliteInfo` → 200，spreadsheetml.sheet，25805 字节，Excel 格式。

### 13. GET /satellites/groundStationInfo —— ✅ 通过
- 返回 5 个地面站，含名称/经纬度/实时关联卫星列表，例：`新疆喀什 [39.47,75.99] conn sats: 8`、`重庆 [29.56,106.55] conn sats: 5`、`雄安 [39.04,115.86] conn sats: 4`。对应说明书 3.3.3 地面站通信数传窗口机制。

### 14. 卫星可用/不可用 —— ✅ 通过
- `GET /satellites/setUnavailable/2` → getSatelliteById/2 读回 `is_available=False`；`GET /satellites/setAvailable/2` → 恢复 `True`。

---

## D. 星簇管理（说明书 3.3.4 + 3.7.9）

### 15. POST /clusters/getClustersByPage —— ✅ 通过
- body `{"page":1,"per_page":5}` → total=157、pages=32，返回条目含 id/name/orbit/payload_resolution/satellite_names/status。

### 16. getOrbits + getInfoByOrbits 联动 —— ✅ 通过
- `GET /clusters/getOrbits` → 10 条轨道（如 `第10轨道红外:0.5,0.75,1.0|光学:...|SAR:...`）。
- `POST /clusters/getInfoByOrbits` body `{"orbits":[10]}` → `resolution_map: {"SAR":[1.0,0.5,0.75],"infrared":[1.0,0.75,0.5],"optical":[0.75,1.0,0.5]}`。

### 17. 新增星簇 POST /clusters/addCluster —— ✅ 通过
- body `{"cluster_name":"TEST_clusterA","orbits":[10],"payload_resolution":{"optical":[1.0]}}` → `星簇添加成功`。
- mysql 验证：t_cluster 新增 id=882（satellite_count=2, payload_resolution=`optical:1.0`），t_cluster_star_relation 自动关联 **Sat_10_11、Sat_10_17**（自动筛选卫星生效）。

### 18. 修改星簇 POST /clusters/updateCluster/882 —— ✅ 通过
- 改名 TEST_clusterA → TEST_clusterA2 → `星簇更新成功`，mysql 读回 name=TEST_clusterA2。

### 19. 星簇禁用/启用（setUnavailableCluster / setAvailableCluster）—— ✅ 已修复并复测通过

> **2026-08-30 复测更新**：根因已修复（`cluster.id` → `cluster.cluster_id`），复测 POST `/clusters/setUnavailableCluster/725` 与 `/clusters/setAvailableCluster/725` 均返回 200 success，mysql 验证 `t_cluster.status` 随禁用/启用正确翻转（0↔1），成员卫星 is_available 内存联动生效。
>
> **以下为修复前的原始失败记录（保留存档）：**
- `POST /clusters/setUnavailableCluster/882` → **500 Internal Server Error**。
- 后端日志 traceback：`AttributeError: 'Cluster' object has no attribute 'id'`，位于 `blueprint/Cluster.py:452` `if cluster.id == cluster_id:`。
- 原因分析：内存中的 `Cluster` 对象（Service/ClusterService.py）只有 `cluster_id` 属性，没有 `id` 属性；遍历第一个星簇即抛异常，导致接口必然 500，`setAvailableCluster`（Cluster.py:480）存在同样问题。
- 影响：星簇禁用/启用及"星簇不可用则成员卫星全部不可用"的联动均不生效（mysql 验证 t_cluster.status 仍为 1，成员卫星 is_available 未变）。
- 按任务要求未修改代码，如实记录。

### 20. 星簇任务迁移 POST /clusters/replan —— ✅ 通过
- body `{"oldCluster":"TEST_clusterA2","newCluster":"Cluster_10_infrared_1.0"}` → 200 `重规划完成`（该星簇无等待执行任务，流程正常走完）。

### 21. 删除星簇 deleteClusterById —— ✅ 通过
- `DELETE /clusters/deleteClusterById/882` → `星簇删除成功`。
- mysql 验证：t_cluster 中 id=882 记录数=0，t_cluster_star_relation 中 cluster_id=882 记录数=0，`TEST_%` 星簇残留=0。两表均已清理。

### 22. GET /clusters/exportAllClusters —— ✅ 通过
- 200，spreadsheetml.sheet，8799 字节，Excel 格式。

### 附：星簇批量导入 submitClusterFile —— ⏭️ 跳过
- 按要求未测（该接口会重建全部星簇），引用上轮结论：已验证通过。

---

## E. 任务管理（说明书 3.3.1 + 3.7.10）

### 23. 新增单任务 POST /tasks/addSingleTask —— ✅ 通过
- TEST_ 点目标任务（光学, 30.0/120.0, 时间窗 2025-06-06~06-07）→ 200，入库 t_new_task id=61（后改名 TEST_point1），状态"等待规划"，随后被自动规划为"等待执行"（分配 Sat_10_17）。
- 注意：任务名由后端自动生成（类型+uuid 前缀），不能通过该接口自定义名称。

### 24. 定时任务 —— ✅ 通过
- 新增 appoint_time 为当前+1 小时的 TEST_appoint1（id=62）→ 入库，appoint_time 正确存储（本地时间转 UTC），状态始终保持"等待规划"、assigned_satellite_name=NULL，整个验证期间（含时间加速到 200 倍阶段）均未进入待规划队列/未被规划。符合说明书"定时任务到点才加入待规划队列"。已删除清理。

### 25. 编辑任务 POST /tasks/updateTask/<id> —— ✅ 通过
- updateTask/61：`{"task_name":"TEST_point1","priority":5}` → 200，mysql 读回 name/priority=5；再改回 priority=3 → 读回 3。改名+改优先级均生效。

### 26. 暂停/恢复 —— ✅ 通过
- `GET /tasks/pauseTask/61` → `{"result":"ok"}`，mysql 读回 status=**暂停**。
- `GET /tasks/startTask/61` → `{"result":"ok"}`，mysql 读回 status=**等待执行**（assigned_satellite_name=Sat_10_17）。

### 27. 手动结束 POST /tasks/manualEndTask —— ✅ 通过
- manualEndTask/61 → 200。mysql 验证：t_new_task 中 id=61 记录数=0；t_old_task 新增 id=61、task_name=TEST_point1、status=**Failed**、assigned_satellite_name=Sat_10_17。归档正确。

### 28. 删除任务 POST(DELETE) /tasks/deleteTask —— ✅ 已修复并复测通过

> **2026-08-30 复测更新**：两点观察均已修复。①查询顺序已调整为先查 t_new_task 再查 t_old_task；②删除新任务改为归档（与 manualEndTask 一致）。复测证据：
> - 正常路径（等待规划）：TEST_ 定时任务 id=68 → DELETE deleteTask/68 → ok；mysql 验证 t_new_task 无记录，**t_old_task 新增 id=68、task_name=TEST_appoint_del、status=Failed**。
> - 正常路径（等待执行）：TEST_ 任务 id=69（已分配 Sat_7_10）→ 删除 → ok；t_old_task 归档 id=69 status=Failed 且 assigned_satellite_name=Sat_7_10 保留。
> - 边界：deleteTask/99999 → `任务不存在`。
> - id 碰撞场景：mysql 直插两表同 id=900001 的 TEST_ 记录后调用删除 → 接口优先命中新表，归档插入因旧表同 id 主键冲突报 500（事务回滚），**旧表归档记录完好未被误删**（修复前会直接误删旧表记录），无数据丢失。碰撞测试数据已清理。
>
> **以下为修复前的原始记录（保留存档）：**
> - 新建任务 id=64（等待规划状态）→ `DELETE /tasks/deleteTask/64` → "ok"。mysql 验证：t_new_task 无、**t_old_task 也无**（删除为硬删除，不归档；与"删除后归档"的预期描述有出入，代码实现如此）。
> - 观察 1：deleteTask 先查 t_old_task 再查 t_new_task，若新旧表出现相同 id，会优先误删旧表记录（id 碰撞场景）。
> - 观察 2：首次删除测试（id=63）时任务在创建后数秒内即被规划执行并自动归档，导致紧随其后的 updateTask 返回 404——说明规划-执行链路响应很快，非接口缺陷。

### 29. 区域目标拆分 —— ✅ 通过
- 提交 4 坐标点 TEST_ 区域目标任务（id=66）→ 后端日志可见拆分为子任务 **6601、6602、6603、6604**（=父ID 66×100+序号，与说明书一致），规划后父任务状态"等待执行"，assigned_satellite_name=`Sat_7_10,Sat_10_17,Sat_10_17,Sat_7_10`，子任务被逐个下发执行。
- 边界场景：第一次提交的时间窗（2025-06-10）超出卫星轨迹预计算窗口，4 个子任务无可行卫星选项，父任务按预期转入 t_old_task（Failed）——失败路径行为正确。已删除清理。

### 30. 任务导出 —— ✅ 已修复并复测通过

> **2026-08-30 复测更新**：`exportTask`/`exportOldTask` 已改为与批量导出一致的 pandas+openpyxl 生成 Excel，前端 `Shuxing.vue` 下载文件名同步改为 .xlsx。复测证据：
> - `GET /tasks/exportTask/68` → 200，Content-Type: spreadsheetml.sheet，`file` 识别为 Microsoft Excel 2007+ ✅
> - `GET /tasks/exportOldTask/32` → 200，同为 Excel ✅
> - 边界：exportTask/99999（不存在）→ 404 `未找到指定任务` ✅
> - 前端回归：`shot-audit.cjs` 全页面巡检 ZERO ERRORS ✅
>
> **以下为修复前的原始记录（保留存档）：**
> - `GET /tasks/exportAllNewTasks` → 200，Excel（9455 字节）✅
> - `GET /tasks/exportAllOldTasks` → 200，Excel（6013 字节）✅
> - `GET /tasks/exportTask/66` → 200 但 **Content-Type: text/plain（txt 文本，538 字节）**，不是 Excel。代码实现如此（exportTask/exportOldTask 单条导出均为 txt），与"返回 Excel"的预期不符。

### 31. Excel 批量导入 POST /tasks/addTasks —— ✅ 通过
- 按 generate_tasks 解析列构造 xlsx（列：优先级/是否紧急/任务类型/载荷类型/分辨率/时间范围`(2025,6,6,8,0,0)-(2025,6,7,8,0,0)`/目标区域`[31.0, 121.0]`/星簇/周期/定时时间/云层厚度），上传 → "ok"。
- mysql 验证：新增 id=67（点目标/optical/[31.0, 121.0]/等待规划）。随后 deleteTask/67 清理，t_new_task 无残留。

---

## F. 任务规划与执行闭环（说明书 3.3.2）

### 32. 启动规划 —— ✅ 通过
- 规划为自动触发：客户端连接使 is_trace=True 后，运控主循环自动从队列取任务并并行运行**贪心/蚁群/遗传**三种算法（backend_run.log 可见三种算法的 plans、评分 score≈70.9-71.2、"完成！调度了 1/1 个任务"）。
- startTask 接口已在第 26 项验证（恢复暂停任务）。TEST_point1 从"等待规划"自动变为"等待执行"并分配 Sat_10_17，规划链路正常。

### 33. GET /getPlanningEvaluation —— ✅ 通过
- 返回 6 条规划评估记录，含三算法指标：`ant_colony/genetic/greedy` 各有 imaging_quality、resource_utilization、overall_battery_cost、overall_storage_cost、schedule 明细，另有 task_satisfaction、duration 等汇总键。

### 34. GET /getClusterData/<cluster_name> —— ✅ 通过
- `getClusterData/Cluster_10_optical_1.0` → 6 个时序点，含 cluster_battery_utilization=0.0341、cluster_storage_utilization、cluster_tasks_count、time_occupancy_ratio 等。

### 35. POST /exportSchedule/{greedy|ant|genetic|schedule} —— ✅ 已修复并复测通过

> **2026-08-30 复测更新**：`number` 参数已改为可选，不传时自动取 output_plans/ 下最新批次目录；文件不存在时返回友好中文提示。复测证据：
> - 不带参数 → 4 个均 **200**，返回 output_plans/1/ 下方案 txt（卫星任务调度报告）✅（前端 Xingneng.vue 本就传 `{number:null}`，四个导出按钮无需改动即可直接下载最新批次方案）
> - 带 `{"number":1}` → 200（旧行为兼容）✅
> - 边界：`{"number":"abc"}` → 400 `number必须为整数`；`{"number":999}`（批次不存在）→ 404 `暂无规划方案数据，请先执行任务规划` ✅
>
> **以下为修复前的原始记录（保留存档）：**
> - 不带参数 → 4 个均 404 `{"error":"文件不存在"}`（方案文件实际按规划批次存于 `output_plans/<批次号>/` 子目录，首次规划后 `output_plans/greedy.txt` 根路径不存在）。
> - 带 `{"number":1}` → 4 个均 200，返回 txt 方案文件（各约 744-747 字节，内容为"卫星任务调度报告"）。
> - 结论：功能可用，但必须传批次号 number；不传时 404 的提示不够友好（建议提示需要 number 参数）。

### 36. 任务执行闭环 —— ✅ 通过
- `myvenv/bin/python run_mock_clients.py` 拉起 2 个模拟客户端连接 127.0.0.1:9999（lsof 确认 2 对 ESTABLISHED）。
- 为加快验证临时将时间倍速调至 200（`/changeTimeMultiple`，测后已恢复 1）。
- backend_run.log 证据链完整：`发送任务给客户端： 6602 / 6603`（socket 下发）→ 客户端执行 → 后端收到 `{'task_id': 6603, 'result': '成功'}`、`{'task_id': 6602, 'result': '成功'}` → 父任务 66 状态变为"正在执行"。
- 下发→执行→结果回收闭环验证通过。验证后已停掉模拟客户端。

---

## G. UI 巡检（说明书 3.7 章界面）

### 37. 前端逐页巡检 —— ✅ 通过
- `cd frontend && NODE_PATH=/tmp/shot/node_modules node shot-audit.cjs`，exit=0，输出 **=== DONE: ZERO ERRORS ===**（无 console.error / pageerror）。
- 覆盖页面（截图均在 frontend/ 下，13:06-13:07 生成）：shot_login.png、shot_login_select.png、shot_Weixing.png、shot_Weixing_select.png、shot_Weixing_dialog.png、shot_shuxing.png、shot_shezhi.png、shot_Xingcu.png、shot_Yongli.png、shot_Xingneng.png、shot_system_settings.png、shot_ground_station.png、shot_satellite_network.png、shot_portal.png。

---

## 汇总统计

| 结论 | 数量 | 项目 |
|---|---|---|
| ✅ 通过 | 34 | 1-18, 20-29, 31-34, 36, 37 |
| ❌ 失败→✅已修复 | 1→0 | 19（星簇禁用/启用 500，已于 2026-08-30 修复并复测通过） |
| ⚠️ 部分→✅已修复 | 2→0 | 30（单任务导出 txt→xlsx）、35（exportSchedule 批次号可选+友好提示），均于 2026-08-30 修复并复测通过 |
| ⏭️ 跳过 | 1 | 星簇批量导入 submitClusterFile（按要求不测，引用上轮结论"已验证通过"） |

### 失败项详情
- **第 19 项（星簇禁用/启用）**：`POST /clusters/setUnavailableCluster/<id>` 与 `/clusters/setAvailableCluster/<id>` 必然 500。根因：`blueprint/Cluster.py` 第 452/480 行访问 `cluster.id`，但内存 `Cluster` 对象（`Service/ClusterService.py`）的属性名是 `cluster_id`。后果：星簇级禁用/启用及"成员卫星联动不可用"功能完全不可用（数据库 status 与卫星 is_available 均未改变）。

### 其他观察（非失败项，供参考）
1. ~~`/simulateParameters` 更新仿真起止时间后不会重置当前仿真时钟 now_time。~~ **已于 2026-08-30 修复并复测通过**：提交新参数时在 task_lock 内将 now_time（含 satellite_network.now_time）重置为新 start_time。复测：提交前 current_time=2025-06-06 00:40:35，POST date1=2026-08-30 09:00:00 后 current_time=2026-08-30 01:00:00（本地转 UTC）=新 start_time；非法日期仍 400。测后已恢复默认窗口与权重。
2. ~~`deleteTask` 先查旧表后查新表，新旧表 id 碰撞时可能误删旧表记录；且删除等待规划任务为硬删除，不会归档到 t_old_task。~~ **已于 2026-08-30 修复并复测通过**，详见第 28 项复测更新。
3. 任务名由后端自动生成（类型+uuid），addSingleTask/addTasks 均不能自定义任务名。
4. 后端必须等到 NODES=2 个客户端连接 9999 后，卫星状态计算与任务规划才开始（`_start_socket_server` 永久阻塞 accept）。**2026-08-30 评估结论：设计使然，不改代码**。任务通过 `client=(num//3+1)%NODES` 分配到各客户端、由实物客户端 SatClient.exe 执行并回传结果，`check_execute_tasks` 要求 client_sockets 非空才下发；等齐全部客户端再启动可保证每颗卫星对应的客户端在线，属分布式执行架构预期行为。客户端断开后 `_receive_results` recv 返回空即 break 正常退出，不会永久阻塞/空转。config.py 注释"最大客户端连接数"语义略不准（实为必需客户端数），但行为一致，不做修改。
